{
	"name": "oauth.ara.team",
	"description": "Next.js on Cloudflare Pages just for Auth.js",
	"license": "GPL-3.0-only",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"local": "next dev --turbopack",
		"dev": "next dev --turbopack",
		"build": "next build",
		"start": "next start",
		"lint": "next lint",
		"pages:build": "yarn next-on-pages",
		"preview": "yarn pages:build && wrangler pages dev",
		"deploy": "yarn pages:build && wrangler pages deploy"
	},
	"dependencies": {
		"next": "15.3.3",
		"react": "^19.0.0",
		"react-dom": "^19.0.0"
	},
	"devDependencies": {
		"@cloudflare/next-on-pages": "1",
		"vercel": "^42.3.0",
		"wrangler": "^4.19.1"
	}
}
